# ============================================
# Конфигурация переменных окружения
# ============================================
#
# ВАЖНО: Скопируйте этот файл в .env и заполните реальными значениями
# Файл .env находится в .gitignore и НИКОГДА не должен попадать в git!
#
# Использование:
#   1. Скопируйте: cp .env.example .env
#   2. Отредактируйте .env с вашими реальными учетными данными
# ============================================

# Подключение к базе данных PostgreSQL
# Формат: postgres://username:password@host:port/database?sslmode=disable
# Пример: postgres://myuser:mypassword@localhost:5432/pr_reviewer?sslmode=disable
DATABASE_URL=postgres://postgres:postgres@localhost:5432/pr_reviewer?sslmode=disable

# Учетные данные PostgreSQL (используются docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pr_reviewer

# Конфигурация сервера
PORT=8080

# Примечание: переменная MIGRATIONS_PATH не требуется для docker-compose
# Она устанавливается автоматически в docker-entrypoint.sh